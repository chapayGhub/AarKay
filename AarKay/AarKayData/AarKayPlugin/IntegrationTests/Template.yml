name: Template
isPlugin: false
properties:
    - name|String
    - properties|[ArgModel]?
    - computedProperties|[ArgModel]?
computedProperties:
    - allProperties|[ArgModel]?
    - requiredProperties|[ArgModel]?

